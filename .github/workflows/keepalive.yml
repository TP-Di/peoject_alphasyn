name: Keepalive

on:
  schedule:
    - cron: "0 0 1 * *"  # 1-е число каждого месяца, 00:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "keepalive $(date -u)"
